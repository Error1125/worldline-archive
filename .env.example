# Worldline Archive — 环境变量样例
# 复制为 .env 后按需填写。全部可选：不配任何项，站点照常构建。

# ── 前端（Astro 构建期，PUBLIC_ 前缀会打进产物） ──────────────────
# 控制台后端（Cloudflare Worker）地址。
# 不配时可在 /admin/login 页面手填，会存 localStorage（优先级更高）。
PUBLIC_ADMIN_API_BASE=

# ── scripts/sync-github.ts（本地 / CI 拉取 GitHub 数据用） ────────
# GitHub 用户名，默认 Error1125
GITHUB_USER=
# 只读 token（可选）。匿名限流 60 次/时；带 token 5000 次/时。
# 注意：这里的 token 只在本机/CI 使用，与 Worker 的 GITHUB_TOKEN（写权限）是两回事。
GITHUB_TOKEN=

# ── Worker 端（不在这里配！） ─────────────────────────────────────
# ADMIN_SECRET / SESSION_SECRET / GITHUB_TOKEN(写权限) 属于 Cloudflare Worker secrets，
# 用 `npx wrangler secret put <NAME>` 注入，见 worker/README.md。
