{
  "name": "worldline-admin-worker",
  "private": true,
  "version": "5.0.0",
  "description": "Worldline Archive Admin API (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
