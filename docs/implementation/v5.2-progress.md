# Worldline Archive v5.2 实施进度

> 基线：当前本地 `main`；参考包仅用于逐项提取实现思路，绝不整包覆盖。

## 已完成模块

- 基线审计与完整交接资料阅读。
- Anime 卡片详情导航与独立外链；卡片键盘焦点与按压反馈。
- Projects 真实 GitHub snapshot 合并、历史假 stars/forks 清理、详情页基础扩展。
- 发布状态按 commit SHA 查询、no-store、防缓存轮询及前台检查反馈。
- Bangumi Worker 同步：分页读取、个人字段保护、单次 Git Data commit、Cron 入口与 Secret 隔离。
- Footer 真实技术栈与统一 email `mailto:` 规范化。
- Admin Projects Manager：repo / 站内 Project 列表、visibility 调整、repo 创建 Draft；仅预填 `github.owner` / `github.repo`。
- Admin Bangumi Settings：用户名、范围、频率、保存与立即同步结果/错误；前端不接收 Token。
- 共用 QQ / WeChat ContactPop：二维码、账号复制、外点/Esc 关闭、焦点返回与移动端布局。
- Loader 首帧静态 shell、sessionStorage 同步判定、正文交叉淡入及 5 秒超时兜底。
- 内容 View Transition、Anime cover / Project title shared transition、筛选/搜索/排序 FLIP、Lightbox 动画和错误状态。

## 进行中模块

- 无。

## 未完成模块

- 无功能性未完成项。

## 修改文件

- `src/content/config.ts`、`src/lib/github/repos.ts`、`src/lib/content.ts`。
- `src/components/cards/AnimeCard.astro`、`src/components/cards/ProjectCard.astro`、`src/pages/projects/[slug].astro`。
- `src/content/projects/*.md`、`src/lib/admin/api.ts`、`src/components/admin/screens.tsx`。
- `worker/src/github.ts`、`worker/src/index.ts`、`worker/src/bangumi.ts`、`worker/src/content-files.ts`、`worker/src/settings-schema.ts`、`worker/wrangler.toml`。
- `src/config/bangumi.json`、`src/lib/contact.ts`、`src/components/home/ProfileCard.astro`、`src/pages/about.astro`、`src/components/layout/Footer.astro`、`src/styles/global.css`。
- `src/components/common/ContactPop.astro`、`src/components/admin/AdminApp.tsx`、`src/pages/admin/projects.astro`、`src/pages/admin/settings/bangumi.astro`、`worker/src/frontmatter.ts`、`src/components/detail/RecordDetailLayout.astro`、`src/components/layout/SiteLayout.astro`、`src/components/effects/WorldlineLoader.tsx`。

## 验证结果

- `pnpm check`：0 errors。
- `pnpm build`：成功，生成 Anime / Project 详情静态路由。
- `pnpm --dir worker exec tsc --noEmit`：成功。
- `npx wrangler deploy --dry-run`：成功（未部署）。
- 实际浏览器回归：首次/同 session 首页、直达 Anime Detail、Day/Night、搜索/筛选/排序、375px 移动宽度、ContactPop、Lightbox 均通过。

## 已知问题

- 当前工作树含若干用户未跟踪的历史交接文档；保持不触碰。
- Bangumi 必须由用户配置用户名，并可选设置 `BANGUMI_TOKEN` Secret；当前空配置不会执行同步。
- 未使用真实 Worker Secret / GitHub Token，因此 Bangumi 空配置与上游 API 失败分支以代码路径和 Worker 类型检查验证；需要配置后在已认证 Admin 中做线上联调。

## 下一步

1. 配置 Bangumi 用户名及可选 `BANGUMI_TOKEN` 后进行受控线上联调。
2. 在真实 GitHub Actions 部署后观察一次 buildSha + 页面可达性链路。
